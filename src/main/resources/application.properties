spring.application.name=chat-service
server.port=${SERVER_PORT:8086}

spring.datasource.url=${CHAT_DB_URL:jdbc:postgresql://localhost:5432/sanosysalvos}
spring.datasource.username=${CHAT_DB_USER:postgres}
spring.datasource.password=${CHAT_DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

jwt.secret=${JWT_SECRET:c2Fub3N5c2Fsdm9zLXNlY3JldC1rZXktcGFyYS1qd3QtMjAyNg==}
jwt.expiration=${JWT_EXPIRATION:86400000}

eureka.client.service-url.defaultZone=${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true